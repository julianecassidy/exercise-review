"use strict"; 

/** Takes 2 strings and returns a boolean. True if the characters in string 1 are
 * are subsequence of the characters in string 2.
 */
function isSubsequence(s1, s2) {
    if (s1.length > s2) return false;
    let i = 0;
    while (i < s2.length) {
        if (s1[i] === s2[i]) {
            testSubstring(s1, )
        }
    }
}

function testSubstring(s1, sub2) {

}
